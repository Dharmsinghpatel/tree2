import { SafeMediaPipe } from './safe-media.pipe';

describe('SafeMediaPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeMediaPipe();
    expect(pipe).toBeTruthy();
  });
});
